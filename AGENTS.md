# AI Agents Guidelines

## Read Order

1. `CLAUDE.md`
2. `AGENTS.md`

## Beads Workflow

- Read `CLAUDE.md` first, then this file.
- Start sessions with `bd dolt pull || true` and `bd ready --json`.
- Claim work before implementation with `bd show <id> --json` and `bd update <id> --claim --json`.
- Create self-contained beads at creation time with `--description`, `--context`, and `--notes`.
- `bd note <id> "progress" --json` for durable progress.
- `bd remember "..." --key <name> --json` for durable repo facts.
- For investigations, incidents, or regressions, use evidence-first beads: repro, observed evidence, likely fix surface when justified, and acceptance that can falsify the theory.
- Decompose obvious epics immediately and wire sequencing with `bd dep`.
- Prefer explicit close reasons like `completed`, `superseded`, `already implemented`, or `invalidated by better evidence`.
- When better evidence arrives, update or close stale contradictory beads instead of leaving both live.
- `bd close <id> --reason "completed" --json` only on real completion.
- End sessions with `bd dolt push`.

This file is auto-generated by n8nac. Run the following to generate the full n8nac protocol:

```bash
npx --yes n8nac init
```

This will create:
- `n8nac-config.json` — instance connection config
- Update this file with the complete n8nac protocol (GitOps sync, research, validation, testing, error classification)

Until then, see `@AGENTS.md` for general agent instructions (Beads task tracking, session completion protocol).
